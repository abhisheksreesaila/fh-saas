# 🤖 AI Interface Orchestrator


<!-- WARNING: THIS FILE WAS AUTOGENERATED! DO NOT EDIT! -->

<a
href="https://github.com/abhisheksreesaila/fh-saas/blob/main/fh_saas/utils_ai_interface.py#L62"
target="_blank" style="float:right; font-size:smaller">source</a>

### ArtifactRef

``` python

def ArtifactRef(
    id:str, tenant_id:str, content_type:str, size_bytes:int, filename:Optional=None
)->None:

```

------------------------------------------------------------------------

<a
href="https://github.com/abhisheksreesaila/fh-saas/blob/main/fh_saas/utils_ai_interface.py#L50"
target="_blank" style="float:right; font-size:smaller">source</a>

### JobRecord

``` python

def JobRecord(
    id:str, tenant_id:str, job_type:str, state:JobState, payload:Dict=<factory>, result:Optional=None,
    error:Optional=None, progress:int=0
)->None:

```

------------------------------------------------------------------------

<a
href="https://github.com/abhisheksreesaila/fh-saas/blob/main/fh_saas/utils_ai_interface.py#L27"
target="_blank" style="float:right; font-size:smaller">source</a>

### AIInterfaceConfig

``` python

def AIInterfaceConfig(
    max_concurrency:int=1, default_poll_interval_ms:int=1200, status_endpoint_template:str='/api/jobs/{job_id}',
    result_endpoint_template:str='/api/jobs/{job_id}/result', max_upload_mb:int=100, request_timeout_seconds:int=900,
    storage_mode:str='db_blob'
)->None:

```

------------------------------------------------------------------------

<a
href="https://github.com/abhisheksreesaila/fh-saas/blob/main/fh_saas/utils_ai_interface.py#L17"
target="_blank" style="float:right; font-size:smaller">source</a>

### JobState

``` python

def JobState(
    args:VAR_POSITIONAL, kwds:VAR_KEYWORD
):

```

*str(object=’’) -\> str* str(bytes_or_buffer\[, encoding\[, errors\]\])
-\> str

Create a new string object from the given object. If encoding or errors
is specified, then the object must expose a data buffer that will be
decoded using the given encoding and error handler. Otherwise, returns
the result of object.\_\_str\_\_() (if defined) or repr(object).
encoding defaults to sys.getdefaultencoding(). errors defaults to
‘strict’.

------------------------------------------------------------------------

<a
href="https://github.com/abhisheksreesaila/fh-saas/blob/main/fh_saas/utils_ai_interface.py#L89"
target="_blank" style="float:right; font-size:smaller">source</a>

### RealtimePublisher

``` python

def RealtimePublisher(
    args:VAR_POSITIONAL, kwargs:VAR_KEYWORD
):

```

*Base class for protocol classes.*

Protocol classes are defined as::

    class Proto(Protocol):
        def meth(self) -> int:
            ...

Such classes are primarily used with static type checkers that recognize
structural subtyping (static duck-typing).

For example::

    class C:
        def meth(self) -> int:
            return 0

    def func(x: Proto) -> int:
        return x.meth()

    func(C())  # Passes static type check

See PEP 544 for details. Protocol classes decorated with
@typing.runtime_checkable act as simple-minded runtime protocols that
check only the presence of given attributes, ignoring their type
signatures. Protocol classes can be generic, they are defined as::

    class GenProto[T](Protocol):
        def meth(self) -> T:
            ...

------------------------------------------------------------------------

<a
href="https://github.com/abhisheksreesaila/fh-saas/blob/main/fh_saas/utils_ai_interface.py#L83"
target="_blank" style="float:right; font-size:smaller">source</a>

### ArtifactStore

``` python

def ArtifactStore(
    args:VAR_POSITIONAL, kwargs:VAR_KEYWORD
):

```

*Base class for protocol classes.*

Protocol classes are defined as::

    class Proto(Protocol):
        def meth(self) -> int:
            ...

Such classes are primarily used with static type checkers that recognize
structural subtyping (static duck-typing).

For example::

    class C:
        def meth(self) -> int:
            return 0

    def func(x: Proto) -> int:
        return x.meth()

    func(C())  # Passes static type check

See PEP 544 for details. Protocol classes decorated with
@typing.runtime_checkable act as simple-minded runtime protocols that
check only the presence of given attributes, ignoring their type
signatures. Protocol classes can be generic, they are defined as::

    class GenProto[T](Protocol):
        def meth(self) -> T:
            ...

------------------------------------------------------------------------

<a
href="https://github.com/abhisheksreesaila/fh-saas/blob/main/fh_saas/utils_ai_interface.py#L76"
target="_blank" style="float:right; font-size:smaller">source</a>

### JobStore

``` python

def JobStore(
    args:VAR_POSITIONAL, kwargs:VAR_KEYWORD
):

```

*Base class for protocol classes.*

Protocol classes are defined as::

    class Proto(Protocol):
        def meth(self) -> int:
            ...

Such classes are primarily used with static type checkers that recognize
structural subtyping (static duck-typing).

For example::

    class C:
        def meth(self) -> int:
            return 0

    def func(x: Proto) -> int:
        return x.meth()

    func(C())  # Passes static type check

See PEP 544 for details. Protocol classes decorated with
@typing.runtime_checkable act as simple-minded runtime protocols that
check only the presence of given attributes, ignoring their type
signatures. Protocol classes can be generic, they are defined as::

    class GenProto[T](Protocol):
        def meth(self) -> T:
            ...

------------------------------------------------------------------------

<a
href="https://github.com/abhisheksreesaila/fh-saas/blob/main/fh_saas/utils_ai_interface.py#L71"
target="_blank" style="float:right; font-size:smaller">source</a>

### InferenceBackend

``` python

def InferenceBackend(
    args:VAR_POSITIONAL, kwargs:VAR_KEYWORD
):

```

*Base class for protocol classes.*

Protocol classes are defined as::

    class Proto(Protocol):
        def meth(self) -> int:
            ...

Such classes are primarily used with static type checkers that recognize
structural subtyping (static duck-typing).

For example::

    class C:
        def meth(self) -> int:
            return 0

    def func(x: Proto) -> int:
        return x.meth()

    func(C())  # Passes static type check

See PEP 544 for details. Protocol classes decorated with
@typing.runtime_checkable act as simple-minded runtime protocols that
check only the presence of given attributes, ignoring their type
signatures. Protocol classes can be generic, they are defined as::

    class GenProto[T](Protocol):
        def meth(self) -> T:
            ...

<a
href="https://github.com/abhisheksreesaila/fh-saas/blob/main/fh_saas/utils_ai_interface.py#L147"
target="_blank" style="float:right; font-size:smaller">source</a>

### register_ai_routes

``` python

def register_ai_routes(
    app, interface:Interface, submit_handler:NoneType=None, status_handler:NoneType=None,
    result_handler:NoneType=None
):

```

*Route orchestrator seam for AI interfaces.*

Caller wires concrete handlers from queue/runtime modules.

------------------------------------------------------------------------

<a
href="https://github.com/abhisheksreesaila/fh-saas/blob/main/fh_saas/utils_ai_interface.py#L108"
target="_blank" style="float:right; font-size:smaller">source</a>

### Interface

``` python

def Interface(
    fn:Callable, inputs:List, outputs:List, name:str='AI Interface', config:Optional=None
):

```

*Declarative AI interface wrapper for fh-saas.*

Phase 1 scope: - Captures fn/inputs/outputs metadata - Exposes route
registration metadata - Builds polling contract for HTMX clients

------------------------------------------------------------------------

<a
href="https://github.com/abhisheksreesaila/fh-saas/blob/main/fh_saas/utils_ai_interface.py#L93"
target="_blank" style="float:right; font-size:smaller">source</a>

### validate_ai_interface_config

``` python

def validate_ai_interface_config(
    config:AIInterfaceConfig
)->List:

```

*Call self as a function.*
